zerops:
  # ---------------- victim A: no safety net (control group) ----------------
  # No deploy.readinessCheck and no run.healthCheck - both omitted deliberately.
  # A dead or half-dead container here stays in rotation until someone notices.
  - setup: naive
    build:
      base: nodejs@22
      deployFiles:
        - spike
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      envVariables:
        PORT: "3000"
        VARIANT: naive
      start: node spike/victim.js

  # ---------------- victim B: identical code, real config ----------------
  # Same file, same build. The entire difference is the six lines below.
  - setup: hardened
    build:
      base: nodejs@22
      deployFiles:
        - spike
    deploy:
      readinessCheck:
        httpGet:
          port: 3000
          path: /healthz
        # These are Go durations, NOT integers. The docs example shows bare ints
        # (`failureTimeout: 60`) and the API rejects them:
        #   "cannot unmarshal !!int `30` into time.Duration"
        failureTimeout: 30s
        # retryPeriod is clamped to <10s, 1h>. Anything below 10s is rejected.
        retryPeriod: 10s
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      envVariables:
        PORT: "3000"
        VARIANT: hardened
      start: node spike/victim.js
      healthCheck:
        httpGet:
          port: 3000
          path: /healthz
        failureTimeout: 15s
        disconnectTimeout: 10s

  # ---------------- portless load generator ----------------
  # No ports block at all - this is a long-running worker, not a web service.
  - setup: gen
    build:
      base: nodejs@22
      deployFiles:
        - spike
    run:
      base: nodejs@22
      envVariables:
        RPS_PER_TARGET: "10"
        NAIVE_URL: http://naive:3000
        HARDENED_URL: http://hardened:3000
      start: node spike/gen.js
